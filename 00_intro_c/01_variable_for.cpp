#include <stdio.h>
#include <iostream>

using namespace std;

int main (int argc, char *argv[]) {

	int numero = 0;
	int secreto = 8;
	
	for (int i = 1; numero =! secreto;
	
		cout << "Dame un numero: ",
		cin >> numero,
		cout << "el numero es " << numero << endl,
		
		
		if (numero < secreto)
			cout << "El número secreto es mayor." << endl,
		
		else if (numero > secreto)
			cout << "El número secreto es menor." << endl,
		i++
	)
		
	
	cout << "has acertado el número" << endl;
	cout << "Has hacertado el numero en " << i << " intentos." << endl;
	
}
